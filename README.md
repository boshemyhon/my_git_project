# ✈️ Система керування аеропортом

## 📘 Опис проєкту
**Система керування аеропортом** — це навчальний програмний проєкт, створений для демонстрації принципів об’єктно-орієнтованого програмування з використанням інтерфейсів та класів.  
Метою є автоматизація основних процесів роботи аеропорту: **управління рейсами**, **реєстрація пасажирів** та **облік багажу**.

---

## 🧩 Основний функціонал
- Управління рейсами (інформація, розклад, статуси).  
- Реєстрація пасажирів і видача посадкових талонів.  
- Облік, маркування та відстеження багажу.  
- Просте консольне відображення результатів.

---

## 🧱 Архітектура проєкту

src/
├── interfaces/
│ ├── IFlight.java # Інтерфейс для керування рейсами
│ ├── IPassenger.java # Інтерфейс для роботи з пасажирами
│ └── IBaggage.java # Інтерфейс для управління багажем
│
├── classes/
│ ├── Flight.java # Клас, що реалізує інтерфейс IFlight
│ ├── Passenger.java # Клас, що реалізує інтерфейс IPassenger
│ └── Baggage.java # Клас, що реалізує інтерфейс IBaggage
│
└── Main.java # Головний файл для демонстрації роботи

---

## 🧭 Інтерфейси

### **IFlight**
Визначає функціонал для управління рейсами.

| Метод | Опис |
|--------|------|
| `getFlightInfo()` | Повертає повну інформацію про рейс (номер, напрям, час вильоту). |
| `getSchedule()` | Повертає розклад рейсу. |
| `getStatus()` | Повертає статус рейсу (On Time, Delayed, Cancelled). |

---

### **IPassenger**
Містить методи для роботи з пасажирами.

| Метод | Опис |
|--------|------|
| `registerPassenger()` | Реєструє пасажира на рейс. |
| `issueBoardingPass()` | Видає посадковий талон. |
| `managePassengerList()` | Керує списком пасажирів (додавання, видалення, перегляд). |

---

### **IBaggage**
Описує методи для обліку та відстеження багажу.

| Метод | Опис |
|--------|------|
| `registerBaggage()` | Реєструє багаж у системі. |
| `markBaggage()` | Створює унікальний ідентифікатор (бірку). |
| `trackBaggage()` | Відстежує статус багажу (Checked In, In Transit, Delivered). |

---

## 🚀 Класи

### **Flight**
Реалізує інтерфейс `IFlight`.

**Поля:**
- `flightNumber` — номер рейсу  
- `destination` — пункт призначення  
- `departureTime` — час вильоту  
- `status` — поточний стан рейсу  

**Методи:**
- `getFlightInfo()` — повертає повну інформацію про рейс.  
- `getSchedule()` — повертає розклад.  
- `getStatus()` — повертає статус рейсу.

---

### **Passenger**
Реалізує інтерфейс `IPassenger`.

**Поля:**
- `name` — ім’я пасажира  
- `passportNumber` — номер паспорта  
- `flight` — об’єкт класу `Flight`  
- `boardingPassIssued` — стан видачі посадкового талона  

**Методи:**
- `registerPassenger()` — додає пасажира на рейс.  
- `issueBoardingPass()` — позначає, що талон видано.  
- `managePassengerList()` — керує списком пасажирів.

---

### **Baggage**
Реалізує інтерфейс `IBaggage`.

**Поля:**
- `baggageId` — унікальний ідентифікатор  
- `owner` — посилання на пасажира  
- `weight` — вага багажу  
- `status` — поточний стан  

**Методи:**
- `registerBaggage()` — додає багаж до системи.  
- `markBaggage()` — генерує бірку.  
- `trackBaggage()` — відображає статус багажу.

---

## 🧪 Демонстрація роботи
Файл **`Main.java`** містить приклад використання:
1. Створення об’єкта рейсу.  
2. Реєстрація пасажира.  
3. Реєстрація багажу.  
4. Виведення інформації у консоль.  

---

## 🧰 Використані технології
- **Java** — мова реалізації класів і інтерфейсів  
- **Git + GitHub** — система контролю версій і публічне сховище  
- **Markdown** — формат документації

---

## ⚙️ Як запустити
1. Клонувати репозиторій:
   ```bash
   git clone https://github.com/boshemigon/my_git_progect
